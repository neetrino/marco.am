/** True black price ink — bypasses globals.css remap of `.text-black` to `--marco-slate`. */
export const PRODUCT_CARD_PRICE_CLASS = 'product-card-price';

/** List-row card shell — enables light text on dark surfaces in `.dark` only. */
export const PRODUCT_CARD_LIST_SURFACE_CLASS = 'product-card-list';
