/**
 * Home hero — Figma MARCO (node 305:2146) primary asset.
 * Inset values are applied via `.hero-section-inset` in `globals.css`.
 */
export const HERO_PRIMARY_IMAGE_SRC = '/assets/hero/hero-yellow-brick-wall.png' as const;
