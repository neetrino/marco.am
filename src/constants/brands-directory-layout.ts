/**
 * `/brands` grid — every logo is drawn inside this fixed cell so mixed assets read at similar size.
 */
export const BRANDS_DIRECTORY_LOGO_CELL_HEIGHT_PX = 120;

export const BRANDS_DIRECTORY_LOGO_CELL_MAX_WIDTH_PX = 400;

/** Card shell — headroom around the logo cell. */
export const BRANDS_DIRECTORY_CARD_MIN_HEIGHT_PX = 224;

export const BRANDS_DIRECTORY_LOGO_IMAGE_CLASS = 'max-h-full max-w-full object-contain';
