import { HomeHeroReelsFallback } from './HomeHeroReelsFallback';

/** Home route — show hero-shaped shell immediately while server sections stream. */
export default function Loading() {
  return <HomeHeroReelsFallback />;
}
