/** Desktop related carousel — one page = four cards (4-4-4 pagination). */
export const RELATED_PRODUCTS_DESKTOP_CARDS_PER_PAGE = 4;

/** Mobile / tablet related carousel — two cards per page. */
export const RELATED_PRODUCTS_MOBILE_CARDS_PER_PAGE = 2;

/** `lg` breakpoint — four-across layout matches home special-offers rail. */
export const RELATED_PRODUCTS_DESKTOP_BREAKPOINT_PX = 1024;
