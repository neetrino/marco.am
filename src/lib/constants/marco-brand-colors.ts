/** Brand CTA fill — RGB(63, 84, 102). Used on home pills, category control, and «see more» actions. */
export const MARCO_SLATE_HEX = '#3F5466' as const;

/** Circular icon actions (wishlist, compare) on product cards. */
export const MARCO_SLATE_ICON_ACTION_CLASS =
  'border-[var(--marco-slate)] bg-[var(--marco-slate)] text-white hover:brightness-95 active:brightness-90';

/** Compact circular icon actions without border (special-offer cards). */
export const MARCO_SLATE_ICON_CHIP_CLASS =
  'bg-[var(--marco-slate)] text-white shadow-sm transition-[filter] hover:brightness-95 active:brightness-90';

/** Pill CTAs — empty states, add to cart, browse actions. */
export const MARCO_SLATE_PILL_BUTTON_CLASS =
  '!bg-[var(--marco-slate)] !text-white hover:brightness-95 active:brightness-90';